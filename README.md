# Study Atlas

Expandable GitHub Pages learning hub. Current topics: Pop2Piano and DeepSeek V4.

Future-agent workflow and context-saving notes are in `AGENTS.md`.
Regenerate search with:

```bash
python3 scripts/build-search-index.py
```
